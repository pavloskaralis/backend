function checkAffectedBookings(doctorId, licenseId) {
  // Placeholder for booking check logic
  // TODO: enqueue a job or run logic to find bookings affected by license changes
  console.log(`Check affected bookings for doctor ${doctorId} and license ${licenseId}`);
}

module.exports = { checkAffectedBookings };
